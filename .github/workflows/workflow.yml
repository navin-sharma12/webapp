name: CI

on:
  pull_request:    
    branches: [main]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    strategy: 
      matrix:
        node-version: [18.x]

    - name: secrets
      env:
        env: ${{ secrets.DB_DIALECT }}
        env: ${{ secrets.DB_HOST }}
        env: ${{ secrets.DB_PORT}}
        env: ${{ secrets.DB_USER }}
        env: ${{ secrets.DB_PASSWORD }}
        env: ${{ secrets.DB_DATABASE }}

    steps:
      - uses: actions/checkout@v1
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
      - run: npm install
      - run: npm install chai
      - run: npm install mocha
      - run: npm run test 