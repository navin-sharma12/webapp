name: CI

on:
  pull_request:    
    branches: [main]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    strategy: 
      matrix:
        node-version: [18.x]

    steps:
      - uses: actions/checkout@v1
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
      - run: npm init -y
      - run: npm install sequelize
      - run: npm install chai
      - run: npm install mocha
      - run: npm install cors
      - run: npm install dotenv
      - run: npm install express
      - run: npm install supertest
      - run: npm install mysql
      - run: npm install mysql2
      - run: |
          echo "Dialect: ${{ secrets.env.DIALECT }}"
          echo "Host: ${{ secrets.env.HOST }}"
          echo "Port: ${{ secrets.env.PORT }}"
          echo "User: ${{ secrets.env.USER }}"
          echo "Password: ${{ secrets.env.PASSWORD }}"
          echo "Database: ${{ secrets.env.DATABASE }}"
      - run: npm run test 
        env:
          dialect: ${{ secrets.DIALECT }}
          host: ${{ secrets.HOST }}
          port: ${{ secrets.PORT}}
          user: ${{ secrets.USER }}
          password: ${{ secrets.PASSWORD }}
          database: ${{ secrets.DATABASE }}